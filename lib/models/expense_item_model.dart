class ExpenseItemModel {
  final String image, title, date, price;

  const ExpenseItemModel({
    required this.image,
    required this.title,
    required this.date,
    required this.price,
  });
}
